#include <stdio.h>
#include <stdlib.h>
#include "ladder.h"

int main(void)
{
	printf("Starting up");
	SoftItem* psi = initialize("/home/providence/mindterm");
	printf("Directory is %s \n", psi->disk_location);
	exit(EXIT_SUCCESS);
}
